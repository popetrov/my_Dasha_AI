import * as base from "./base";
import { Account } from "../../account";
import { Logger } from "../../log";
import { AudioRequestStream, IExternalTtsClient } from "../types/external-tts-protocol";
import * as external_tts_grpc_pb from "./generated/external-client-tts_grpc_pb";
export declare class ExternalTtsClient extends base.Client<external_tts_grpc_pb.ExternalClientTtsProtocolClient> implements IExternalTtsClient {
    constructor(account: Account, log: Logger);
    sendAudioData(request: {
        requestId: string;
        messageId: string;
        instanceId: string;
        audio: {
            format: string;
            data: Uint8Array;
        };
    }): Promise<void>;
    sendAudioError(request: {
        requestId: string;
        messageId: string;
        instanceId: string;
        error: {
            type: string;
            message: string;
            details: string;
        };
    }): Promise<void>;
    audioRequestStream(request: {
        instanceId: string;
    }): AudioRequestStream;
}
