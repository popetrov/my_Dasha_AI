import EventEmitter from "eventemitter3";
export interface IStream<TEvents extends EventEmitter.ValidEventTypes> extends EventEmitter<TEvents> {
    cancel(): void;
}
export interface IClient {
    close(): void;
}
