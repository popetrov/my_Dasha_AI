import * as base from "./base";
import { Account } from "../../account";
import { Logger } from "../../log";
import { ITextProtocolClient, MessageStream } from "../types/text-protocol";
import * as sdk_grpc_pb from "./generated/sdk_grpc_web_pb";
export declare class TextProtocolClient extends base.Client<sdk_grpc_pb.TextProtocolClient> implements ITextProtocolClient {
    constructor(account: Account, log: Logger);
    sendText(request: {
        jobId: string;
        text: string;
    }): Promise<void>;
    sendClose(request: {
        jobId: string;
    }): Promise<void>;
    messageStream(request: {
        jobId: string;
    }): MessageStream;
}
