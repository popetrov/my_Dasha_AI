import * as base from "./base";
import { Account } from "../../account";
import { Logger } from "../../log";
import * as nlu_service_grpc_pb from "./generated/nlu-service_grpc_pb";
import { INluServiceClient, RecognitionResponse, AvailableSkillsResponse, TrainProgressStream } from "../types/nlu";
export declare class NluServiceClient extends base.Client<nlu_service_grpc_pb.NluServiceClient> implements INluServiceClient {
    constructor(account: Account, log: Logger);
    train(nluContent: Record<string, unknown>, language: string): TrainProgressStream;
    recognize(skillIds: string[], language: string, text: string): Promise<RecognitionResponse>;
    getAvailableSkills(language: string): Promise<AvailableSkillsResponse>;
}
